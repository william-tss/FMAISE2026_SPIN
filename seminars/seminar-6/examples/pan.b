	switch (t->back) {
	default: Uerror("bad return move");
	case  0: goto R999; /* nothing to undo */

		 /* PROC :init: */

	case 3: // STATE 1
		;
		now.v8 = trpt->bup.oval;
		;
		goto R999;

	case 4: // STATE 2
		;
		now.v9 = trpt->bup.oval;
		;
		goto R999;

	case 5: // STATE 3
		;
		now.v11 = trpt->bup.oval;
		;
		goto R999;

	case 6: // STATE 4
		;
		now.v12 = trpt->bup.oval;
		;
		goto R999;

	case 7: // STATE 5
		;
		now.v13 = trpt->bup.oval;
		;
		goto R999;

	case 8: // STATE 6
		;
		now.v15 = trpt->bup.oval;
		;
		goto R999;

	case 9: // STATE 7
		;
		now.v17 = trpt->bup.oval;
		;
		goto R999;

	case 10: // STATE 8
		;
		now.v29 = trpt->bup.oval;
		;
		goto R999;

	case 11: // STATE 9
		;
		now.time = trpt->bup.oval;
		;
		goto R999;

	case 12: // STATE 10
		;
		now.tB1B3 = trpt->bup.oval;
		;
		goto R999;

	case 13: // STATE 11
		;
		now.tB2B3 = trpt->bup.oval;
		;
		goto R999;

	case 14: // STATE 12
		;
		now.tB3B4 = trpt->bup.oval;
		;
		goto R999;

	case 15: // STATE 13
		;
		now.tB4B5 = trpt->bup.oval;
		;
		goto R999;

	case 16: // STATE 14
		;
		now.tB5B7 = trpt->bup.oval;
		;
		goto R999;

	case 17: // STATE 15
		;
		now.tH = trpt->bup.oval;
		;
		goto R999;

	case 18: // STATE 16
		;
		now.tC6 = trpt->bup.oval;
		;
		goto R999;

	case 19: // STATE 17
		;
		now.tC7 = trpt->bup.oval;
		;
		goto R999;

	case 20: // STATE 18
		;
		now.tP1 = trpt->bup.oval;
		;
		goto R999;

	case 21: // STATE 19
		;
		now.tP2 = trpt->bup.oval;
		;
		goto R999;

	case 22: // STATE 20
		;
		now.batches = trpt->bup.oval;
		;
		goto R999;

	case 23: // STATE 21
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 24: // STATE 22
		;
		now.px[1] = trpt->bup.oval;
		;
		goto R999;

	case 25: // STATE 23
		;
		now.px[2] = trpt->bup.oval;
		;
		goto R999;

	case 26: // STATE 24
		;
		now.px[3] = trpt->bup.oval;
		;
		goto R999;

	case 27: // STATE 25
		;
		now.px[4] = trpt->bup.oval;
		;
		goto R999;

	case 28: // STATE 26
		;
		now.px[5] = trpt->bup.oval;
		;
		goto R999;

	case 29: // STATE 27
		;
		now.px[6] = trpt->bup.oval;
		;
		goto R999;

	case 30: // STATE 28
		;
		now.px[7] = trpt->bup.oval;
		;
		goto R999;

	case 31: // STATE 29
		;
		now.px[8] = trpt->bup.oval;
		;
		goto R999;

	case 32: // STATE 30
		;
		now.px[9] = trpt->bup.oval;
		;
		goto R999;

	case 33: // STATE 31
		;
		now.px[10] = trpt->bup.oval;
		;
		goto R999;

	case 34: // STATE 32
		;
		now.px[11] = trpt->bup.oval;
		;
		goto R999;

	case 35: // STATE 33
		;
		now.px[12] = trpt->bup.oval;
		;
		goto R999;

	case 36: // STATE 34
		;
		now.mix = trpt->bup.oval;
		;
		goto R999;

	case 37: // STATE 35
		;
		now.heat = trpt->bup.oval;
		;
		goto R999;

	case 38: // STATE 36
		;
		now.p1 = trpt->bup.oval;
		;
		goto R999;

	case 39: // STATE 37
		;
		now.p2 = trpt->bup.oval;
		;
		goto R999;

	case 40: // STATE 38
		;
		now.B1 = trpt->bup.oval;
		;
		goto R999;

	case 41: // STATE 39
		;
		now.B2 = trpt->bup.oval;
		;
		goto R999;

	case 42: // STATE 40
		;
		now.B3 = trpt->bup.oval;
		;
		goto R999;

	case 43: // STATE 41
		;
		now.B4 = trpt->bup.oval;
		;
		goto R999;

	case 44: // STATE 42
		;
		now.B5 = trpt->bup.oval;
		;
		goto R999;

	case 45: // STATE 43
		;
		now.B6 = trpt->bup.oval;
		;
		goto R999;

	case 46: // STATE 44
		;
		now.B7 = trpt->bup.oval;
		;
		goto R999;

	case 47: // STATE 45
		;
		;
		delproc(0, now._nr_pr-1);
		;
		goto R999;

	case 48: // STATE 46
		;
		;
		delproc(0, now._nr_pr-1);
		;
		goto R999;

	case 49: // STATE 47
		;
		;
		delproc(0, now._nr_pr-1);
		;
		goto R999;

	case 50: // STATE 48
		;
		;
		delproc(0, now._nr_pr-1);
		;
		goto R999;

	case 51: // STATE 49
		;
		;
		delproc(0, now._nr_pr-1);
		;
		goto R999;

	case 52: // STATE 50
		;
		;
		delproc(0, now._nr_pr-1);
		;
		goto R999;

	case 53: // STATE 51
		;
		;
		delproc(0, now._nr_pr-1);
		;
		goto R999;

	case 54: // STATE 52
		;
		;
		delproc(0, now._nr_pr-1);
		;
		goto R999;

	case 55: // STATE 53
		;
		;
		delproc(0, now._nr_pr-1);
		;
		goto R999;

	case 56: // STATE 54
		;
		;
		delproc(0, now._nr_pr-1);
		;
		goto R999;

	case 57: // STATE 55
		;
		;
		delproc(0, now._nr_pr-1);
		;
		goto R999;

	case 58: // STATE 56
		;
		;
		delproc(0, now._nr_pr-1);
		;
		goto R999;

	case 59: // STATE 58
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC Advance */

	case 60: // STATE 3
		;
		((P11 *)_this)->i = trpt->bup.ovals[1];
		((P11 *)_this)->minstep = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 62: // STATE 11
		;
		((P11 *)_this)->i = trpt->bup.ovals[2];
		((P11 *)_this)->minstep = trpt->bup.ovals[1];
	/* 0 */	((P11 *)_this)->minstep = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 63: // STATE 11
		;
		((P11 *)_this)->i = trpt->bup.oval;
		;
		goto R999;

	case 64: // STATE 11
		;
		((P11 *)_this)->i = trpt->bup.oval;
		;
		goto R999;

	case 65: // STATE 12
		;
	/* 0 */	((P11 *)_this)->i = trpt->bup.oval;
		;
		;
		goto R999;

	case 66: // STATE 17
		;
		now.time = trpt->bup.oval;
		;
		goto R999;

	case 67: // STATE 22
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC Control */
;
		;
		
	case 69: // STATE 3
		;
		((P10 *)_this)->j = trpt->bup.ovals[1];
		((P10 *)_this)->i = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 70: // STATE 5
		;
		((P10 *)_this)->postcon = trpt->bup.oval;
		;
		goto R999;

	case 71: // STATE 6
		;
	/* 0 */	((P10 *)_this)->postcon = trpt->bup.oval;
		;
		;
		goto R999;

	case 72: // STATE 9
		;
		now.v8 = trpt->bup.ovals[1];
		now.px[1] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 73: // STATE 12
		;
		now.v9 = trpt->bup.ovals[1];
		now.px[2] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 74: // STATE 16
		;
		now.mix = trpt->bup.ovals[2];
		now.v8 = trpt->bup.ovals[1];
		now.px[3] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 75: // STATE 20
		;
		now.mix = trpt->bup.ovals[2];
		now.v9 = trpt->bup.ovals[1];
		now.px[4] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 76: // STATE 23
		;
		now.v11 = trpt->bup.ovals[1];
		now.px[5] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 77: // STATE 26
		;
		now.v12 = trpt->bup.ovals[1];
		now.px[6] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 78: // STATE 30
		;
		now.v13 = trpt->bup.ovals[2];
		now.heat = trpt->bup.ovals[1];
		now.px[7] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 79: // STATE 33
		;
		now.v15 = trpt->bup.ovals[1];
		now.px[8] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 80: // STATE 36
		;
		now.v17 = trpt->bup.ovals[1];
		now.px[9] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 81: // STATE 39
		;
		now.v29 = trpt->bup.ovals[1];
		now.px[10] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 82: // STATE 42
		;
		now.p1 = trpt->bup.ovals[1];
		now.px[11] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 83: // STATE 45
		;
		now.p2 = trpt->bup.ovals[1];
		now.px[12] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 84: // STATE 53
		;
		((P10 *)_this)->j = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->j = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 85: // STATE 56
		;
		((P10 *)_this)->i = trpt->bup.ovals[2];
		((P10 *)_this)->j = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->j = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 86: // STATE 59
		;
	/* 0 */	((P10 *)_this)->i = trpt->bup.oval;
		;
		;
		goto R999;

	case 87: // STATE 64
		;
		((P10 *)_this)->i = trpt->bup.oval;
		;
		goto R999;

	case 88: // STATE 66
		;
		((P10 *)_this)->last = trpt->bup.ovals[1];
		((P10 *)_this)->j = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 89: // STATE 68
		;
		((P10 *)_this)->precon = trpt->bup.oval;
		;
		goto R999;

	case 90: // STATE 69
		;
	/* 0 */	((P10 *)_this)->precon = trpt->bup.oval;
		;
		;
		goto R999;

	case 91: // STATE 72
		;
		now.v8 = trpt->bup.ovals[1];
		now.px[1] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 92: // STATE 75
		;
		now.v9 = trpt->bup.ovals[1];
		now.px[2] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 93: // STATE 79
		;
		now.mix = trpt->bup.ovals[2];
		now.v8 = trpt->bup.ovals[1];
		now.px[3] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 94: // STATE 83
		;
		now.mix = trpt->bup.ovals[2];
		now.v9 = trpt->bup.ovals[1];
		now.px[4] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 95: // STATE 86
		;
		now.v11 = trpt->bup.ovals[1];
		now.px[5] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 96: // STATE 89
		;
		now.v12 = trpt->bup.ovals[1];
		now.px[6] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 97: // STATE 93
		;
		now.v13 = trpt->bup.ovals[2];
		now.heat = trpt->bup.ovals[1];
		now.px[7] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 98: // STATE 96
		;
		now.v15 = trpt->bup.ovals[1];
		now.px[8] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 99: // STATE 99
		;
		now.v17 = trpt->bup.ovals[1];
		now.px[9] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 100: // STATE 102
		;
		now.v29 = trpt->bup.ovals[1];
		now.px[10] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 101: // STATE 105
		;
		now.p1 = trpt->bup.ovals[1];
		now.px[11] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 102: // STATE 108
		;
		now.p2 = trpt->bup.ovals[1];
		now.px[12] = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 103: // STATE 112
		;
		((P10 *)_this)->last = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->precon = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 104: // STATE 118
		;
		((P10 *)_this)->j = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->j = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 105: // STATE 121
		;
		((P10 *)_this)->i = trpt->bup.ovals[2];
		((P10 *)_this)->j = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->j = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 106: // STATE 124
		;
	/* 0 */	((P10 *)_this)->i = trpt->bup.oval;
		;
		;
		goto R999;
;
		;
		
	case 108: // STATE 175
		;
		now.cycle = trpt->bup.ovals[3];
		now.v8 = trpt->bup.ovals[2];
		now.px[1] = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->last = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 4);
		goto R999;

	case 109: // STATE 175
		;
		now.cycle = trpt->bup.ovals[3];
		now.v9 = trpt->bup.ovals[2];
		now.px[2] = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->last = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 4);
		goto R999;

	case 110: // STATE 175
		;
		now.cycle = trpt->bup.ovals[4];
		now.mix = trpt->bup.ovals[3];
		now.v8 = trpt->bup.ovals[2];
		now.px[3] = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->last = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 5);
		goto R999;

	case 111: // STATE 175
		;
		now.cycle = trpt->bup.ovals[4];
		now.mix = trpt->bup.ovals[3];
		now.v9 = trpt->bup.ovals[2];
		now.px[4] = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->last = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 5);
		goto R999;

	case 112: // STATE 175
		;
		now.cycle = trpt->bup.ovals[3];
		now.v11 = trpt->bup.ovals[2];
		now.px[5] = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->last = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 4);
		goto R999;

	case 113: // STATE 175
		;
		now.cycle = trpt->bup.ovals[3];
		now.v12 = trpt->bup.ovals[2];
		now.px[6] = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->last = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 4);
		goto R999;

	case 114: // STATE 175
		;
		now.cycle = trpt->bup.ovals[4];
		now.v13 = trpt->bup.ovals[3];
		now.heat = trpt->bup.ovals[2];
		now.px[7] = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->last = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 5);
		goto R999;

	case 115: // STATE 175
		;
		now.cycle = trpt->bup.ovals[3];
		now.v15 = trpt->bup.ovals[2];
		now.px[8] = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->last = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 4);
		goto R999;

	case 116: // STATE 175
		;
		now.cycle = trpt->bup.ovals[3];
		now.v17 = trpt->bup.ovals[2];
		now.px[9] = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->last = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 4);
		goto R999;

	case 117: // STATE 175
		;
		now.cycle = trpt->bup.ovals[3];
		now.v29 = trpt->bup.ovals[2];
		now.px[10] = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->last = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 4);
		goto R999;

	case 118: // STATE 175
		;
		now.cycle = trpt->bup.ovals[3];
		now.p1 = trpt->bup.ovals[2];
		now.px[11] = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->last = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 4);
		goto R999;

	case 119: // STATE 175
		;
		now.cycle = trpt->bup.ovals[3];
		now.p2 = trpt->bup.ovals[2];
		now.px[12] = trpt->bup.ovals[1];
	/* 0 */	((P10 *)_this)->last = trpt->bup.ovals[0];
		;
		;
		ungrab_ints(trpt->bup.ovals, 4);
		goto R999;

	case 120: // STATE 175
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 121: // STATE 175
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;
;
		;
		
	case 123: // STATE 183
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC Pump2 */
;
		;
		
	case 125: // STATE 3
		;
		now.B6 = trpt->bup.oval;
		;
		goto R999;

	case 126: // STATE 5
		;
		now.B6 = trpt->bup.oval;
		;
		goto R999;
;
		;
		
	case 128: // STATE 20
		;
		now.cycle = trpt->bup.ovals[2];
		now.tP2 = trpt->bup.ovals[1];
		now.B2 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 129: // STATE 20
		;
		now.cycle = trpt->bup.ovals[2];
		now.tP2 = trpt->bup.ovals[1];
		now.B2 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 130: // STATE 20
		;
		now.cycle = trpt->bup.ovals[1];
		now.tP2 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 131: // STATE 20
		;
		now.cycle = trpt->bup.ovals[1];
		now.tP2 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 133: // STATE 24
		;
		now.B6 = trpt->bup.oval;
		;
		goto R999;

	case 134: // STATE 26
		;
		now.B6 = trpt->bup.oval;
		;
		goto R999;
;
		;
		
	case 136: // STATE 40
		;
		now.cycle = trpt->bup.ovals[1];
		now.B2 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 137: // STATE 40
		;
		now.cycle = trpt->bup.ovals[1];
		now.B2 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 138: // STATE 40
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 139: // STATE 40
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 140: // STATE 45
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC Pump1 */
;
		;
		
	case 142: // STATE 3
		;
		now.B7 = trpt->bup.oval;
		;
		goto R999;

	case 143: // STATE 5
		;
		now.B7 = trpt->bup.oval;
		;
		goto R999;
;
		;
		
	case 145: // STATE 20
		;
		now.cycle = trpt->bup.ovals[2];
		now.tP1 = trpt->bup.ovals[1];
		now.B1 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 146: // STATE 20
		;
		now.cycle = trpt->bup.ovals[2];
		now.tP1 = trpt->bup.ovals[1];
		now.B1 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 147: // STATE 20
		;
		now.cycle = trpt->bup.ovals[1];
		now.tP1 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 148: // STATE 20
		;
		now.cycle = trpt->bup.ovals[1];
		now.tP1 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 150: // STATE 24
		;
		now.B7 = trpt->bup.oval;
		;
		goto R999;

	case 151: // STATE 26
		;
		now.B7 = trpt->bup.oval;
		;
		goto R999;
;
		;
		
	case 153: // STATE 40
		;
		now.cycle = trpt->bup.ovals[1];
		now.B1 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 154: // STATE 40
		;
		now.cycle = trpt->bup.ovals[1];
		now.B1 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 155: // STATE 40
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 156: // STATE 40
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 157: // STATE 45
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC Cool7 */
;
		;
		
	case 159: // STATE 13
		;
		now.cycle = trpt->bup.ovals[2];
		now.tC7 = trpt->bup.ovals[1];
		now.B7 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 160: // STATE 13
		;
		now.cycle = trpt->bup.ovals[2];
		now.tC7 = trpt->bup.ovals[1];
		now.B7 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 161: // STATE 13
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 162: // STATE 13
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;
;
		;
		
	case 164: // STATE 25
		;
		now.cycle = trpt->bup.ovals[1];
		now.B7 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 165: // STATE 25
		;
		now.cycle = trpt->bup.ovals[1];
		now.B7 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 166: // STATE 25
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 167: // STATE 25
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 168: // STATE 30
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC Cool6 */
;
		;
		
	case 170: // STATE 13
		;
		now.cycle = trpt->bup.ovals[2];
		now.tC6 = trpt->bup.ovals[1];
		now.B6 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 171: // STATE 13
		;
		now.cycle = trpt->bup.ovals[2];
		now.tC6 = trpt->bup.ovals[1];
		now.B6 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 172: // STATE 13
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 173: // STATE 13
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;
;
		;
		
	case 175: // STATE 25
		;
		now.cycle = trpt->bup.ovals[1];
		now.B6 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 176: // STATE 25
		;
		now.cycle = trpt->bup.ovals[1];
		now.B6 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 177: // STATE 25
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 178: // STATE 25
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 179: // STATE 30
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC B5toB7 */
;
		;
		
	case 181: // STATE 3
		;
		now.B5 = trpt->bup.oval;
		;
		goto R999;
;
		;
		
	case 183: // STATE 20
		;
		now.cycle = trpt->bup.ovals[2];
		now.tB5B7 = trpt->bup.ovals[1];
		now.B7 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 184: // STATE 20
		;
		now.cycle = trpt->bup.ovals[2];
		now.tB5B7 = trpt->bup.ovals[1];
		now.B7 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 185: // STATE 20
		;
		now.cycle = trpt->bup.ovals[2];
		now.tB5B7 = trpt->bup.ovals[1];
		now.B7 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 186: // STATE 20
		;
		now.cycle = trpt->bup.ovals[1];
		now.tB5B7 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 187: // STATE 20
		;
		now.cycle = trpt->bup.ovals[1];
		now.tB5B7 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 189: // STATE 24
		;
		now.B5 = trpt->bup.oval;
		;
		goto R999;
;
		;
		
	case 191: // STATE 38
		;
		now.cycle = trpt->bup.ovals[1];
		now.B7 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 192: // STATE 38
		;
		now.cycle = trpt->bup.ovals[1];
		now.B7 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 193: // STATE 38
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 194: // STATE 38
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 195: // STATE 43
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC Heat5 */
;
		;
		
	case 197: // STATE 3
		;
		now.B5 = trpt->bup.oval;
		;
		goto R999;
;
		;
		;
		;
		
	case 200: // STATE 25
		;
		now.cycle = trpt->bup.ovals[2];
		now.tH = trpt->bup.ovals[1];
		now.B6 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 201: // STATE 25
		;
		now.cycle = trpt->bup.ovals[2];
		now.tH = trpt->bup.ovals[1];
		now.B6 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 202: // STATE 25
		;
		now.cycle = trpt->bup.ovals[2];
		now.tH = trpt->bup.ovals[1];
		now.B6 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 203: // STATE 25
		;
		now.cycle = trpt->bup.ovals[1];
		now.tH = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 204: // STATE 25
		;
		now.cycle = trpt->bup.ovals[1];
		now.tH = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 205: // STATE 25
		;
		now.cycle = trpt->bup.ovals[1];
		now.tH = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 207: // STATE 29
		;
		now.B5 = trpt->bup.oval;
		;
		goto R999;
;
		;
		;
		;
		
	case 210: // STATE 48
		;
		now.cycle = trpt->bup.ovals[1];
		now.B6 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 211: // STATE 48
		;
		now.cycle = trpt->bup.ovals[1];
		now.B6 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 212: // STATE 48
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 213: // STATE 48
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 214: // STATE 48
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 215: // STATE 53
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC B4toB5 */
;
		;
		;
		;
		
	case 218: // STATE 16
		;
		now.cycle = trpt->bup.ovals[3];
		now.tB4B5 = trpt->bup.ovals[2];
		now.B5 = trpt->bup.ovals[1];
		now.B4 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 4);
		goto R999;

	case 219: // STATE 16
		;
		now.cycle = trpt->bup.ovals[3];
		now.tB4B5 = trpt->bup.ovals[2];
		now.B5 = trpt->bup.ovals[1];
		now.B4 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 4);
		goto R999;

	case 220: // STATE 16
		;
		now.cycle = trpt->bup.ovals[2];
		now.tB4B5 = trpt->bup.ovals[1];
		now.B5 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 221: // STATE 16
		;
		now.cycle = trpt->bup.ovals[1];
		now.tB4B5 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 222: // STATE 16
		;
		now.cycle = trpt->bup.ovals[1];
		now.tB4B5 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		;
		;
		
	case 225: // STATE 32
		;
		now.cycle = trpt->bup.ovals[2];
		now.B5 = trpt->bup.ovals[1];
		now.B4 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 226: // STATE 32
		;
		now.cycle = trpt->bup.ovals[2];
		now.B5 = trpt->bup.ovals[1];
		now.B4 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 227: // STATE 32
		;
		now.cycle = trpt->bup.ovals[1];
		now.B5 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 228: // STATE 32
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 229: // STATE 32
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 230: // STATE 37
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC B3toB4 */
;
		;
		
	case 232: // STATE 3
		;
		now.B3 = trpt->bup.oval;
		;
		goto R999;

	case 233: // STATE 19
		;
		now.cycle = trpt->bup.ovals[3];
		now.batches = trpt->bup.ovals[2];
		now.tB3B4 = trpt->bup.ovals[1];
		now.B4 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 4);
		goto R999;

	case 234: // STATE 19
		;
		now.cycle = trpt->bup.ovals[3];
		now.batches = trpt->bup.ovals[2];
		now.tB3B4 = trpt->bup.ovals[1];
		now.B4 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 4);
		goto R999;

	case 235: // STATE 19
		;
		now.cycle = trpt->bup.ovals[2];
		now.batches = trpt->bup.ovals[1];
		now.tB3B4 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 236: // STATE 19
		;
		now.cycle = trpt->bup.ovals[2];
		now.batches = trpt->bup.ovals[1];
		now.tB3B4 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 237: // STATE 19
		;
		now.cycle = trpt->bup.ovals[2];
		now.batches = trpt->bup.ovals[1];
		now.tB3B4 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;
;
		;
		
	case 239: // STATE 23
		;
		now.B3 = trpt->bup.oval;
		;
		goto R999;

	case 240: // STATE 37
		;
		now.cycle = trpt->bup.ovals[1];
		now.B4 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 241: // STATE 37
		;
		now.cycle = trpt->bup.ovals[1];
		now.B4 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 242: // STATE 37
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 243: // STATE 37
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 244: // STATE 37
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 245: // STATE 42
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC B2toB3 */
;
		;
		
	case 247: // STATE 3
		;
		now.B2 = trpt->bup.oval;
		;
		goto R999;

	case 248: // STATE 5
		;
		now.B2 = trpt->bup.oval;
		;
		goto R999;

	case 249: // STATE 18
		;
		now.cycle = trpt->bup.ovals[2];
		now.tB2B3 = trpt->bup.ovals[1];
		now.B3 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 250: // STATE 18
		;
		now.cycle = trpt->bup.ovals[2];
		now.tB2B3 = trpt->bup.ovals[1];
		now.B3 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 251: // STATE 18
		;
		now.cycle = trpt->bup.ovals[1];
		now.tB2B3 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 252: // STATE 18
		;
		now.cycle = trpt->bup.ovals[1];
		now.tB2B3 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 254: // STATE 22
		;
		now.B2 = trpt->bup.oval;
		;
		goto R999;

	case 255: // STATE 24
		;
		now.B2 = trpt->bup.oval;
		;
		goto R999;

	case 256: // STATE 36
		;
		now.cycle = trpt->bup.ovals[1];
		now.B3 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 257: // STATE 36
		;
		now.cycle = trpt->bup.ovals[1];
		now.B3 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 258: // STATE 36
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 259: // STATE 36
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 260: // STATE 41
		;
		p_restor(II);
		;
		;
		goto R999;

		 /* PROC B1toB3 */
;
		;
		
	case 262: // STATE 3
		;
		now.B1 = trpt->bup.oval;
		;
		goto R999;

	case 263: // STATE 5
		;
		now.B1 = trpt->bup.oval;
		;
		goto R999;

	case 264: // STATE 18
		;
		now.cycle = trpt->bup.ovals[2];
		now.tB1B3 = trpt->bup.ovals[1];
		now.B3 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 265: // STATE 18
		;
		now.cycle = trpt->bup.ovals[2];
		now.tB1B3 = trpt->bup.ovals[1];
		now.B3 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 3);
		goto R999;

	case 266: // STATE 18
		;
		now.cycle = trpt->bup.ovals[1];
		now.tB1B3 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 267: // STATE 18
		;
		now.cycle = trpt->bup.ovals[1];
		now.tB1B3 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;
;
		;
		
	case 269: // STATE 22
		;
		now.B1 = trpt->bup.oval;
		;
		goto R999;

	case 270: // STATE 24
		;
		now.B1 = trpt->bup.oval;
		;
		goto R999;

	case 271: // STATE 36
		;
		now.cycle = trpt->bup.ovals[1];
		now.B3 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 272: // STATE 36
		;
		now.cycle = trpt->bup.ovals[1];
		now.B3 = trpt->bup.ovals[0];
		;
		ungrab_ints(trpt->bup.ovals, 2);
		goto R999;

	case 273: // STATE 36
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 274: // STATE 36
		;
		now.cycle = trpt->bup.oval;
		;
		goto R999;

	case 275: // STATE 41
		;
		p_restor(II);
		;
		;
		goto R999;
	}

